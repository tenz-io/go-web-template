verbose: true
env: "local"
app:
  name: "go-web-template"
  port: 8081
  web: "./web"
  secret: "<set in env>"
db:
  host: "localhost"
  port: 3306
  user: "root"
  pass: "<set in env>"
  db: "mytest_db"

